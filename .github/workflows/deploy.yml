name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all apps
        run: |
          pnpm --filter @mobilesurvey/hub build
          pnpm --filter @mobilesurvey/designer build
          pnpm --filter @mobilesurvey/runtime build

      - name: Prepare deployment directory
        run: |
          mkdir -p deploy/mobilesurvey/designer
          mkdir -p deploy/mobilesurvey/respondent
          cp -r apps/hub/dist/* deploy/mobilesurvey/
          cp -r apps/designer/dist/* deploy/mobilesurvey/designer/
          cp -r apps/runtime/dist/* deploy/mobilesurvey/respondent/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
