{
  "name": "@mobilesurvey/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend service implementing the integration interfaces (CMS, sessions, paradata, samples) over SQLite (node:sqlite).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --import tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@mobilesurvey/runtime-engine": "workspace:*",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
