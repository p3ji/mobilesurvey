{
  "name": "@mobilesurvey/metadata-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Metadata registry: extract reusable DDI components from instruments and search them (lexical now; embeddings later).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mobilesurvey/instrument-schema": "workspace:*",
    "@mobilesurvey/runtime-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.6"
  }
}
